meta {
  name: Update Permission
  type: http
  seq: 3
}

put {
  url: {{baseUrl}}/lists/{{listId}}/permissions/{{permissionId}}
  body: json
  auth: inherit
}

body:json {
  {
    "permission": "READ"
  }
}

vars:pre-request {
  listId: 1
  permissionId: 1
}
