# Nome da aplicação
spring.application.name=shoppmate

# Configuração do banco de dados Postgres
spring.datasource.url=jdbc:postgresql://localhost:5432/${DB_SHOPP_MATE:SHOPPMATE}
spring.datasource.username=${DB_USER:user}
spring.datasource.password=${DB_PASS:password}

# Configuração para criação/atualização automática de tabelas
spring.jpa.generate-ddl= false
spring.jpa.hibernate.ddl-auto= none

# Configuração do Flyway
spring.flyway.enabled=true
spring.flyway.baseline-on-migrate=true
spring.flyway.locations=classpath:db/migration

#springdoc.api-docs.path=v3/api-docs
#springdoc.swagger-ui.path=/swagger-ui.html

# 1h in millisecond
jwt.token.expiration=${JWT_TOKEN_EXPIRATION:3600000}

# RSA Key Paths for JWT Encryption (JWE)
# For development, keys are in src/main/resources/certs/
# For production, use absolute file paths (e.g., file:/etc/shoppmate/certs/private_key.pem)
jwt.private-key=classpath:certs/private_key.pem
jwt.public-key=classpath:certs/public_key.pem
